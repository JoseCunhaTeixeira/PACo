# G3: curve QC, per window

Runs on each window's M0 pick, after the picking (an attempt of the `picking` stage in the QC
log, whose curve is saved in PAC's layout, `DispersionCurves_0000.csv`). Today's
`dispersion_quality` metrics, said in the gates' language, then the curve's own rules: the
wavelength range, mode jumps, the air wave, the trend, the points, the uncertainties. Code:
`src/paco/qc/g3_curve.py` over `paco.quality.measure_quality`; thresholds: `curve` in
`qc_config.json` (`CurveThresholds`, with today's `QualityParameters` under `metrics`); the
picking a run starts from: `picking` in the same file (as far as the ridge holds, below); tests:
`tests/test_qc_curve.py` and, for the metrics themselves, `tests/test_quality.py`.

## What it checks

The first four are medians over the pick's tracked points (the image's columns); the rest look
at the curve saved for the inversion, resampled in wavelength, sorted by wavelength.

| Metric | Threshold (default) | Why |
|---|---|---|
| `sharpness` | peak width at half its height above the floor, over a perfect plane wave's for the same window, frequency and velocity: at least 0.8 | A plane wave scores 1; well below it, the peak is a fringe or an edge, not a propagating wave: rejected, no parameter helps at this window length |
| `prominence` | peak height above the floor over its column's median height, over the same for a perfect plane wave (both counted up to 4): at least 0.5 | Barely above the rest of the image for what the window allows: filter to the band (G2's, or G1's usable one), or mute |
| `on_data` | share of points on their column's brightest value (within 10 %): at least 0.6 | The tracker's smoothing drew the curve, not the data: competing ridges; narrow the band |
| `constant_wavelength` | share of points where velocity grows like frequency: at most 0.4 | The edge of what the window resolves, not a dispersion curve: cut the long wavelengths where that stretch starts (`max_wavelength`, in window lengths) |
| `n_points` | at least 2 | With no ridge kept: pick again with a looser mode rule (`mode_min_ratio` 1.2) |
| `aliased_points` | share of points below 2 x the receiver spacing: reported | The picker starts above it; a check that it did |
| `curve_points` | the resampled curve's points: at least `min_points` (5) | Too few for a curve: keep more of the ridge (`min_relative_coherence` x 0.6) |
| `max_jump` | largest relative velocity step between consecutive points: at most 0.3 | Beyond it the pick jumped onto another mode: cut the band where the jump sits (the side with fewer points goes, `picking {"fmax"}` or `fmin`), then track with half the corridor (since 2026-09-25) |
| `air_wave_share` | share of points at 330–345 m/s: at most 0.5 | The air wave, not the ground: mute the arrivals faster than 320 m/s (`muting {mute, 80, 320}`) |
| `trend` | Spearman rank correlation of velocity with wavelength: negative is flagged | Normal dispersion has velocity rising with wavelength; the opposite can be a stiff layer over a softer one: flagged and kept (`Keep`), never rejected |
| `uncertainty` | median Lorentzian uncertainty over the velocity: at most 0.5 | The array's resolving power (receivers, spacing), the same for every window of a line: beyond it the inversion cannot use the curve, and only longer windows help: rejected, not fixable |
| `near_offset` | the distance from the window's nearest shot to its receivers (from `window.json`): at least half the longest wavelength kept | The spec's near-offset rule: closer, the long wavelengths may read slow (near field). Reported and kept (`near_field`), never applied: keeping only far shots lost a third of the demo's curves (the user's decision of milestone 13, `S2_rules.md`) |

Verdict: `reject` when a flag is not fixable (sharpness, uncertainty), `retry` when every flag
can be acted on, `pass` with no flag or kept flags only. `kept`: the curve's band and wavelength
range within the limit, its points there (the resampled curve, 5 to 10 points for a 24-receiver
window: what the inversion gets, not the 110 to 150 tracked columns), the window's receivers.

## Since 2026-09-25: the pick as far as its ridge holds

The user's decisions of 2026-09-25 (`docs/qc_workflow.md`): no cut at 2 window lengths; the pick
is its longest continuous run (a step steeper than |d ln v / d ln f| = 2, or over 2 Hz of
columns too weak to keep, ends it), and keeps no point where a perfect plane wave for the window
stands less than 1 % above its column's median (the window resolves no velocity there);
sharpness and prominence against a plane wave. Measured on `active_p1`, windows every 4
receivers, the pick uncut (figure: `figures/active_p1_short_picks.png`):

- every length's pick is smooth from about 15 to 45 Hz; below 12-15 Hz it scatters, above the
  50 Hz mains line (a dark column: the hum is in phase on every geophone, so its peak sits at
  the grid's top velocity) it follows several branches (`figures/active_p1_dense_picks.png`,
  24 receivers every 4);
- the continuity ends the picks at 12.5 to 16 Hz (medians) on 5 to 11 receivers, 10.5 Hz on 24,
  and at 43 to 45 Hz at the high end; with the contrast floor, the lowest points are 8.5 to
  10 Hz on every length;
- the measured sharpness and prominence equal a perfect plane wave's (ratio 1.00) at every
  length; the fixed limits (prominence 2 of the column's median, sharpness 0.8 of the
  resolvable width) failed every window of 5 to 11 receivers, whose plane wave only reaches a
  prominence of 1.06 to 1.2.

The ladder's 27 trials (`S2_rules.md`) then pass 17 at 5 receivers, 20 at 7 and 9, 23 at 11
(proposed) and 26 at 16; processed whole, the line gives curves on 65, 68, 77, 85 and 98 % of
its windows, the failures mostly within about 3 m of each shot. On the four
24-receiver windows every 24, every curve passes (xmid 2.88 alone keeps a mild inverse trend,
-0.14): the mode jump of xmid 14.88 below sat above the 50 Hz line, where the pick now stops.
With 16 receivers on the whole line, 79 of 81 curves pass G3 and G4.

## On the demo profile, with the 2-window-lengths cut (2026-09-24)

`active_p1`, windows of 24 receivers every 4 receivers (19 windows), PACo's defaults with the
picking's `max_wavelength` at 2 window lengths (2026-09-24):

| Window | Verdict | points | wavelengths (m) | max jump | trend | uncertainty |
|---|---|---|---|---|---|---|
| 2.88 | pass, `inverse_dispersion` | 7 | 3–9 | 0.26 | -0.25 | 0.13 |
| 3.88 | pass, `inverse_dispersion` | 9 | 3–11 | 0.16 | -0.77 | 0.15 |
| 4.88 | pass, `inverse_dispersion` | 9 | 3–11 | 0.17 | -0.18 | 0.15 |
| 5.88 | pass | 9 | 3–11 | 0.09 | +0.07 | 0.15 |
| 6.88 | pass | 9 | 3–11 | 0.09 | +0.05 | 0.15 |
| 7.88–13.88 | pass, `inverse_dispersion` | 8–9 | 3–11 | 0.09–0.24 | -0.85 to -0.31 | 0.14–0.15 |
| 14.88 | retry, `mode_jump` | 8 | 3–10 | 0.53 | +0.26 | 0.14 |
| 15.88 | retry, `mode_jump` | 9 | 3–11 | 0.31 | +0.42 | 0.15 |
| 16.88–18.88 | pass | 10 | 2–11 | 0.10–0.20 | +0.79 to +0.82 | 0.14 |
| 19.88 | pass | 9 | 2–10 | 0.11 | +0.17 | 0.13 |
| 20.88 | pass, `inverse_dispersion` | 5 | 2–6 | 0.08 | -0.80 | 0.08 |

Since milestone 13 the ten windows at the line's ends (2.88 to 6.88, 16.88 to 20.88) also carry
`near_field`, kept: their nearest shot is 0.75 to 4.75 m away, under half their longest
wavelength (3 to 5.5 m).

The four metrics of milestone 11 pass on every window (sharpness 1.3 to 1.5, prominence 3.7 to
6.1, on_data 0.85 to 0.91, constant wavelength 0.16 to 0.23). The curve rules add: the cut at
2 window lengths (11.5 m) leaves 5 to 10 points at 2 to 11 m where the saved curves reached
33 to 81 m; the pick of xmid 14.88 steps by 53 % between two consecutive points (15.88 by 31 %),
the mode jump milestone 11 could not see; the uncertainties sit at 8 to 15 %; no air wave
(0 %); velocity falls with wavelength on 11 of 19 windows (rank correlation -0.18 to -0.85 on
5 to 9 points), kept and reported. The summary the agent reads:

```
G3: 17 pass, 2 retry
G3 inverse_dispersion, xmid 2.88-4.88 (3), 7.88-13.88 (7), 20.88 (1): Velocity falls with
  wavelength (rank correlation -0.25): a stiff layer over a softer one, if the pick is right.
  -> keep: an inverse trend can be geology
G3 mode_jump, xmid 14.88-15.88 (2): A 53% step between consecutive points: the pick jumped
  onto another mode. Track with a narrower corridor. -> picking {"corridor":0.1}
```

With PACo's default windows of 5 receivers (1 m, 92 windows): 67 windows have no ridge at all
within 2 window lengths (2 m: a wavelength that needs frequencies above 100 Hz, where PAC's
default image stops), 25 keep 1 point (`too_few_points`), 4 have uncertainties of 56 %, 4 are
rejected for sharpness: 5 reject, 87 retry, no curve for the line. The finding of milestone 11
stands: the window length must come from the data (the coherence rules). `passive_p1`,
24-receiver windows: 2 windows without a ridge, 1 with constant wavelength and on_data, 1
rejected for sharpness, as `dispersion_quality` found (all bad).

## On analytic images (the tests)

- a clean ridge: pass, a resampled curve of about 23 points at 5 to 27 m, uncertainties 5 to
  50 %, the curve rules all passed;
- a ridge 0.3 times as wide as the window resolves (0.28 of a plane wave's width): `sharpness`,
  rejected;
- a ridge 0.8 above a flat background of 0.6 (a prominence of 1.33, a third of the 4 a
  48-receiver plane wave counts as): `prominence` -> `filtering {iir, 12–38 Hz}`;
- a brighter ridge above M0: `on_data` -> `dispersion {fmin 10, fmax 40}` (the pick's band);
- a ridge at a constant 6 m wavelength: `constant_wavelength` -> `picking {max_wavelength
  0.12}` (the stretch starts at 6 m, 0.12 of the 47 m window);
- no pick: `no_ridge` -> `picking {mode_min_ratio 1.2}`;
- a curve 60 % faster from 30.5 Hz on: `mode_jump` -> `corridor 0.1`;
- a curve at 330 to 345 m/s: `air_wave` -> `muting {mute, 80, 320}`;
- a curve falling from 200 m/s at 20 m to 433 m/s at 11 m: `inverse_dispersion`, kept, pass;
- a curve of 3 points: `too_few_points` -> `min_relative_coherence 0.3`;
- uncertainties of 60 % of the velocity: `uncertain`, rejected;
- a shot 0.75 m from a window whose curve reaches 27.85 m: `near_field`, kept, pass; 20 m away,
  or no geometry given: nothing.

## To judge

- The inverse trend: on 11 of 19 windows with the old cut, carried by points above the 50 Hz
  line (`figures/active_p1_dense_picks.png`); since the pick stops there, 1 of the four
  24-receiver windows and 10 of 81 at 16 receivers, all below 50 Hz, said once over the line by
  G4. Geology (a stiff crust), or the picker? The flag is kept either way.

## Decided

- Measured on 2026-09-25: `max_jump` stays on the resampled curve (26 % at most on good curves
  against 31 and 53 % on jumps); on the tracked points good windows step by up to 47 %.
- Kept as they are (the user, 2026-09-25): the `prominence` fix filters to the band (a mute
  when G2 found none), and `min_points` at 5.
- The window length is the line's, proposed by the ladder and chosen by the agent: sharpness
  and uncertainty reject a window, without a length change of their own.
