# G5: model QC, per window

Runs on each window's inversion (an attempt of the `inversion` stage in the QC log), from the
files the inversion leaves in the window's folder: PAC's models and forward-modelled curves,
the sampler's log, and PACo's own `SeismicInversion_Samples_0000.npz` (every posterior sample,
chain after chain). What it measured is kept next to them as
`SeismicInversion_Measures_0000.json`, for G6 and for you. Code: `src/paco/qc/g5_model.py` over
`paco.inversion.measuring`; thresholds: `model` in `qc_config.json` (`ModelThresholds`); tests:
`tests/test_qc_model.py`, and for the measures `tests/test_inversion_measuring.py`.

The monitored model is PAC's default view, the **smooth median** (sigpipe's layered median
model, smoothed with a shape-preserving spline): G5 judges its fit, and `job_status` reports it.

## What it checks

| Metric | Threshold (default) | Why |
|---|---|---|
| `misfit_short`, `misfit_middle`, `misfit_long` | RMS of (picked - modelled) / uncertainty in each third of the curve's points, sorted by wavelength: at most 2.0 | About 1 is a fit within the errors; judged by band, as PAC's pseudo-section comparison shows the residual by frequency, so a model that fits the long wavelengths and misses the short ones is seen |
| `misfit_layered` | the same, over every point, for the layered median the smooth one comes from: at most 2.0 | Tells a misfit the smoothing makes from one the model makes |
| `rhat` | the largest split R-hat over the parameters (vs1, vs2, thick1, ...), each chain cut in halves: at most 1.1 | The chains sample the same distribution; Gelman's classic limit |
| `acceptance` | the lowest acceptance rate of the chains, from the sampler's log: at least 10 % | A chain that accepts nothing is stuck |
| `samples_per_chain` | models kept per chain after the burn-in: at least 100 | Fewer say nothing about the distribution |
| `at_bound` | the largest share of a parameter's samples within 2 % of its prior's range from a bound: at most 0.1 | A flat posterior puts 2 % there; five times that is a posterior pressing against the bound |
| `useful_depth` | the depth where the spread of the sampled Vs reaches half the prior's spread there (the prior drawn 2,000 times, seeded): reported, never failed | Below it the data say little; "bottom" when they inform the whole model |

| Flag | Action |
|---|---|
| `not_converged` (R-hat, acceptance or samples) | sample twice as long: `inversion {"n_iterations": 2n, "n_burnin_iterations": 2b}` |
| `at_bound`, a Vs bound | widen it: the lowest by x0.8, the highest by x1.25 (`inversion {"vs_layers": [...]}`) |
| `thin_layer`, a thickness at its minimum, above 2 layers | one layer fewer (`n_layers - 1`) |
| `deep_interface`, a thickness at its maximum | kept: the half-space would go deeper than the curve resolves (λmax/2), the limit stays |
| `smoothing_misfit`, the smooth median misfits, the layered one fits | kept: re-inverting cannot fix a smoothing effect (the decision of milestone 13) |
| `underfit`, both misfit | one more layer (`n_layers + 1`), up to 4; then rejected |
| `no_mode`, the layered median has no fundamental mode at some picked points | rejected, at the picking: those points are faster than any mode of a model with a softer layer below (a higher mode may be picked there) |

The cheapest fix first: `underfit` waits until the chains agree and no Vs bound holds the
posterior. The verdict is `reject` with any rejecting flag, `retry` with any change to make,
`pass` otherwise (kept flags only).

## On the demo profile

`active_p1`, windows of 24 receivers every 4 (the 17 curves G3 and G4 passed), bounds derived
from each curve (the checks before S4, see `S4_checks.md`), PAC's effort (100,000 iterations,
5 chains), 2026-09-24:

| Window | G5 | misfit short / middle / long | layered | R-hat | acceptance % | at a bound | useful depth (m) | layered median | G6 |
|---|---|---|---|---|---|---|---|---|---|
| 2.88 | pass, `smoothing_misfit` | 2.17 / 0.41 / 0.16 | 1.05 | 1.002 | 64 | 0.074 | bottom | 219 over 169 at 2.3 m | pass (0.133) |
| 3.88 | pass | 1.48 / 0.29 / 0.11 | 0.62 | 1.002 | 67 | 0.053 | bottom | 239 over 195 at 2.8 m | pass (0.154) |
| 4.88 | pass | 0.88 / 0.24 / 0.10 | 0.53 | 1.008 | 72 | 0.026 | 4.25 | 231 over 222 at 3.6 m | pass (0.132) |
| 5.88 | pass | 0.16 / 0.36 / 0.12 | 0.24 | 1.011 | 75 | 0.029 | 3.55 | 234 over 223 at 3.6 m | pass (0.029) |
| 6.88 | pass | 0.88 / 0.33 / 0.25 | 0.51 | 1.004 | 71 | 0.039 | 3.85 | 239 over 205 at 3.0 m | pass (0.039) |
| 7.88 | pass | 0.67 / 0.44 / 0.13 | 0.40 | 1.007 | 72 | 0.047 | 3.75 | 248 over 210 at 3.1 m | pass (0.045) |
| 8.88 | pass, `smoothing_misfit` | 2.42 / 0.70 / 0.37 | 1.03 | 1.002 | 50 | 0.063 | bottom | 276 over 215 at 2.0 m | pass (0.103) |
| 9.88 | pass | 1.78 / 0.23 / 0.21 | 0.75 | 1.004 | 65 | 0.058 | bottom | 244 over 194 at 2.7 m | pass (0.068) |
| 10.88 | pass | 0.46 / 0.05 / 0.24 | 0.28 | 1.003 | 74 | 0.029 | 3.55 | 223 over 197 at 3.0 m | pass (0.084) |
| 11.88 | pass | 0.41 / 0.18 / 0.10 | 0.25 | 1.005 | 74 | 0.031 | 3.9 | 222 over 197 at 3.3 m | pass (0.062) |
| 12.88 | pass, `smoothing_misfit` | 2.28 / 0.57 / 0.10 | 1.05 | 1.001 | 58 | 0.082 | bottom | 253 over 195 at 2.2 m | pass (0.072) |
| 13.88 | pass, `smoothing_misfit` | 2.80 / 1.02 / 0.14 | 1.41 | 1.003 | 47 | 0.089 | bottom | 274 over 207 at 2.0 m | pass, `model_change` (0.195) |
| 16.88 | pass | 0.46 / 0.14 / 0.28 | 0.34 | 1.012 | 68 | 0.029 | 3.75 | 202 over 220 at 3.8 m | pass (0.146) |
| 17.88 | pass | 1.16 / 0.28 / 0.42 | 0.78 | 1.008 | 66 | 0.032 | 3.6 | 203 over 220 at 3.8 m | pass (0.086) |
| 18.88 | pass | 1.10 / 0.25 / 0.28 | 0.72 | 1.004 | 66 | 0.029 | 3.45 | 201 over 237 at 3.9 m | pass (0.004) |
| 19.88 | pass | 0.62 / 0.47 / 0.19 | 0.47 | 1.001 | 70 | 0.028 | 3.55 | 211 over 200 at 3.3 m | pass (0.062) |
| 20.88 | pass | 0.64 / 0.22 / 0.02 | 0.32 | 1.001 | 72 | 0.038 | 2.25 | 208 over 180 at 1.9 m | pass (0.042) |

All 17 pass. The chains agree everywhere (R-hat at most 1.012, 600 models a chain). The four
windows with a thin stiff top layer (2 to 2.3 m of 219 to 276 m/s over a softer half-space)
fit within the errors as layered models (1.03 to 1.41), and their smooth medians miss the short
wavelengths (2.2 to 2.8): PAC's smoothing spreads the boundary over about 3.5 m. The largest
share of samples at a bound is 8.9 %, always the half-space's lowest Vs on these inverse
windows: the soft half-space would go softer, below what the curves resolve. The summary the
agent reads:

```
G5: 17 pass
G6: 18 pass
G5 smoothing_misfit, xmid 2.88 (1), 8.88 (1), 12.88-13.88 (2): The smooth median misfits 2.2 at
  short wavelengths (3-5 m) where the layered median fits (1.1): PAC's smoothing spreads the
  layer boundaries. -> keep: re-inverting cannot fix a smoothing effect
```

With PAC's own bounds instead (Vs 100 to 1,000 m/s, thicknesses 1 to 10 m, the same effort),
the chains still agree (R-hat at most 1.08, acceptance 22 to 40 %), but the interface sits at
6 to 7.5 m on most windows, beyond the 5.5 m the curves resolve, and the half-space's Vs is the
prior's: its chains' medians lie 5 to 97 % apart, its posterior spread is 60 to 110 % of the
prior's below 7 m. The smooth medians of the thin-layer windows then misfit 2.3 to 3.2.

## On synthetic defects

A curve forward-modelled from a known model, 200 m/s over 350 m/s with the interface at 3 m,
9 points from 20 to 97 Hz (1.9 to 13.6 m), uncertainties of 10 % like the demo's:

| Case | Bounds | G5 | Model found |
|---|---|---|---|
| derived | Vs 148-407, thickness 0.63-6.79 | pass: misfits 0.13 / 0.26 / 0.38, R-hat 1.001, at a bound 3.9 % | 202 over 341 at 3.1 m |
| 3,000 iterations | derived | retry, `not_converged`: R-hat 1.25, 18 models a chain -> 6,000 iterations | 199 over 325 at 2.9 m |
| Vs2 at most 320 (9 % below the truth) | | pass, 6 % at the bound | 201 over 291 at 2.7 m |
| thickness at most 2 m, then 1.5 m | | pass, 6.5 % and 4.4 % at the bound | 190 over 270 at 1.7 m; 184 over 250 at 1.2 m |
| thickness at least 4 m | | pass, 8.4 % at the bound | 209 over 360 at 4.5 m |
| Vs1 at least 230 m/s | changed by the checks before S4 to 148 (above the curve's slowest velocity) | pass | 202 over 346 at 3.3 m |
| Vs2 at most 300 | | sigpipe fails (below) | |

What this says: with uncertainties of 10 %, a bound 10 to 50 % wrong still leaves a model that
fits within the errors (non-uniqueness), and the posterior piles at the bound only mildly (4 to
8 % of the samples, where the demo's own inverse windows put 5 to 9 %). G5 catches sampling too
short, and gross piles; the bounds' real protection is their derivation from the curve before
S4. With Vs2 at most 300 m/s, sigpipe's `inversion_mcmc` fails after sampling
(`operands could not be broadcast together with shapes (600,) (480,)`): one chain kept models
without their predicted curve (bayesbay stores it only once a model's likelihood has been
computed), and sigpipe sums misfits over both. PACo records the window as a failed attempt.

## To judge

- `max_misfit` 2.0 per band: the demo's layered models fit within 0.24 to 1.41; the smooth
  ones reach 2.8 on thin stiff layers, kept by `smoothing_misfit`. Is 2 the right line between
  "within the errors" and "a model that does not explain the curve"?
- `max_at_bound` 10 % at a 2 % edge: it misses bounds 10 to 50 % wrong on this data (above),
  and a lower limit would flag the demo's own inverse windows (5 to 9 % at the half-space's
  lowest Vs), whose re-inversions would slide that bound down twice and then reject a model
  that fits. Keep 10 %, lower it, or judge the half-space's bounds only above the useful depth?
- The Lorentzian uncertainties (10 to 15 % on these windows) set every misfit: a fit within
  them is loose. Should G5 also report PAC's residual (5 % of the velocity or so) as a limit?
- A `no_mode` curve is rejected: points faster than any fundamental mode of the model found. On
  the demo it happens only when the median of the samples is itself inverse enough (xmid 13.88
  with some draws). A higher mode picked at high frequency is the likely cause; should it send
  the window back to the picking with a narrower band instead?
